/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Injecté au build par `define` dans vite.config.js (voir package.json → version). */
declare const __APP_VERSION__: string;
